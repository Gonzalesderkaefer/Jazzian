#!/bin/sh
blockdevice="$(lsblk -i | grep -o sd[a-z][0-9] | wofi --dmenu -p 'Which drive would you like to mount?')"
udisksctl mount -b /dev/$blockdevice && echo "Device mounted successfully" | wofi --dmenu || (udisksctl unmount -b /dev/$blockdevice && echo "removed successfully" | wofi --dmenu)
