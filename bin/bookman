#!/usr/bin/env bash


# Get Bookmark names
bookmark_names=$(jq -r '.Bookmarks | keys[]' bookmarks.jsonc)

# Get Bookmark from user
chosen_bm=$(echo ${bookmark_names} | mdmenu)

# Get URI
uri=$(jq -r ".Bookmarks.${chosen_bm}" bookmarks.jsonc)

echo ${uri}
