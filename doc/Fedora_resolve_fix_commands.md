## Get Resolve to run on Fedora

Installing missing packages:

`sudo dnf install fuse libxcryp-compat`

Removing/moving files:
```
sudo mv libs/libglib-2.0.so* ~/
sudo mv libs/libgio-2.0.so* ~/
sudo mv libs/libgmodule-2.0.so* ~/ 
```
